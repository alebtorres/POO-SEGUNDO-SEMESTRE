import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.ObjectInputStream;
import java.io.ObjectOutputStream;
import java.io.Serializable;
import java.util.Scanner;
import java.io.*;
/**
 *
 * @author katy1
 */
public class Ejemplopersistencia2 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        Persona per= new Persona();
        per.nombre = "Bryan";
        per.edad = 25;
        String archivo ="Datos de personas.txt";
        try{
            ObjectOutputStream ac = new ObjectOutputStream(new FileOutputStream(archivo));
            ac.writeObject(per);
            ac.close();}
        catch(FileNotFoundException e) {
            e.printStackTrace();
        }
        catch (IOException e){
            e.printStackTrace();        
        }
        System.out.println("Se guardo el archivo cons sus datos");
        try{
        ObjectInputStream in = new ObjectInputStream(new FileInputStream(archivo));
        Persona per2 = (Persona) in.readObject();
        System.out.println("Nombre: "+ per2.nombre +" Edad;"+ per2.edad);
        }catch (FileNotFoundException e){
            e.printStackTrace();
        }catch(IOException e){
            e.printStackTrace();
        }catch(ClassNotFoundException e){
            e.printStackTrace();
        }
        System.out.println("El Programa funciona correctamente ");
    }
        }